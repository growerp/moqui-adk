<?xml version="1.0" encoding="UTF-8"?>
<entities xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/entity-definition-3.xsd">

    <!-- Agent configuration per installation (one active record) -->
    <entity entity-name="AdkAgentConfig" package="moqui.adk" use="configuration">
        <field name="adkAgentConfigId" type="id" is-pk="true"/>
        <field name="agentName" type="text-short"/>
        <field name="modelName" type="text-short"/>
        <field name="apiKey" type="text-medium" encrypt="true"/>
        <field name="systemPrompt" type="text-long"/>
        <field name="enabled" type="text-indicator"/>
    </entity>

    <!-- ADK conversation session -->
    <entity entity-name="AdkSession" package="moqui.adk" use="transactional">
        <field name="sessionId" type="id" is-pk="true"/>
        <field name="appName" type="text-short"/>
        <field name="userId" type="id-long"/>
        <field name="state" type="text-very-long"/>
        <field name="createdDate" type="date-time"/>
    </entity>

    <!-- Individual events within a session (full conversation history) -->
    <entity entity-name="AdkSessionEvent" package="moqui.adk" use="transactional">
        <field name="eventId" type="id" is-pk="true"/>
        <field name="sessionId" type="id"/>
        <field name="eventJson" type="text-very-long"/>
        <field name="eventDate" type="date-time"/>
        <relationship type="many-one" related="moqui.adk.AdkSession">
            <key-map field-name="sessionId"/>
        </relationship>
    </entity>

</entities>
